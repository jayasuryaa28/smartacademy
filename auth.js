document.addEventListener("DOMContentLoaded",()=>{if(document.body.dataset.page!=="login")return;let selected="student";const email=document.querySelector("#email"),pass=document.querySelector("#password");document.querySelectorAll(".role-tab").forEach(b=>b.addEventListener("click",()=>{selected=b.dataset.role;document.querySelectorAll(".role-tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");email.value=selected==="student"?"student@smartacademy.demo":selected==="teacher"?"teacher@smartacademy.demo":"admin@smartacademy.demo"}));document.querySelectorAll("[data-demo]").forEach(b=>b.addEventListener("click",()=>{document.querySelector(`[data-role="${b.dataset.demo}"]`).click();pass.value="smart123"}));document.querySelector("#loginForm").addEventListener("submit",e=>{e.preventDefault();const db=getDB(),key=selected==="student"?"students":selected==="teacher"?"teachers":"admins",u=db[key].find(x=>x.email.toLowerCase()===email.value.trim().toLowerCase()&&x.password===pass.value);if(!u){showToast("Invalid credentials.","error");return}setSession({role:selected,userId:u.id,loginAt:new Date().toISOString()});audit("Login",`Logged in as ${selected}`);location.href=`${selected}-dashboard.html`})})
