const Utils={uid(p="id"){return `${p}_${Date.now()}_${Math.random().toString(36).slice(2,8)}`},initials(n=""){return n.split(" ").filter(Boolean).slice(0,2).map(x=>x[0]).join("").toUpperCase()},escape(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]))},formatDate(v,o={day:"2-digit",month:"short",year:"numeric"}){return new Intl.DateTimeFormat("en-IN",o).format(new Date(v))},formatTime(v){return new Intl.DateTimeFormat("en-IN",{hour:"2-digit",minute:"2-digit",hour12:true}).format(new Date(v))},parseTime(t){const [h,m]=t.split(":").map(Number);return h*60+m},nowMinutes(){const d=new Date();return d.getHours()*60+d.getMinutes()},dayKey(d=new Date()){return ["sun","mon","tue","wed","thu","fri","sat"][d.getDay()]},csv(rows){return rows.map(r=>r.map(v=>`"${String(v??"").replace(/"/g,'""')}"`).join(",")).join("\n")},download(n,t,type="text/plain"){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([t],{type}));a.download=n;a.click()},status(p){return p>=85?["Healthy","success"]:p>=75?["Needs Attention","warning"]:["Critical","danger"]}};
